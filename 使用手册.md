## 编译
```bash
cd ~/DynamicCanaryProject/runtime
clang -O0 -c canary_rt.c -o canary_rt.o && clang -O0 -c canary_log.c -o canary_log.o
cd ~/DynamicCanaryProject/build
make clean && make
```

# 打包成静态库
``` bash
ar rcs ../../runtime/libcanary.a ../../runtime/canary_rt.o ../../runtime/canary_log.o
```

## 测试插桩
``` bash
cd ~/DynamicCanaryProject/test/output
# 生成 test_vuln.ll 文件
clang -O0 -Xclang -disable-O0-optnone -S -emit-llvm ../src/test_vuln.c -o test_vuln.ll
# 生成 test_protected.ll 文件
opt -S -load-pass-plugin=../../build/libDynamicCanary.so -passes="dyn-canary" < test_vuln.ll > test_protected.ll
# 生成 vuln_app 可执行文件
clang ../src/test_vuln.c -fno-stack-protector -no-pie -o vuln_app
# 生成 protected_app 可执行文件
clang  -no-pie test_protected.ll ../../runtime/canary_rt.c ../../runtime/canary_log.c -o protected_app
cd ~/DynamicCanaryProject/test/output
python3 ../scripts/exp.py
```

## 测试选择性插桩
``` bash
cd ~/DynamicCanaryProject/test/output
# 生成 test_selective.ll 文件
clang -O0 -Xclang -disable-O0-optnone -S -emit-llvm ../src/test_selective.c -o test_selective.ll
# 生成 test_protected_selective.ll 文件
opt -S -load-pass-plugin=../../build/libDynamicCanary.so -passes="dyn-canary" < test_selective.ll > test_selective_prot.ll
# 生成 selective_app 可执行文件
clang test_selective_prot.ll ../../runtime/canary_rt.c ../../runtime/canary_log.c -no-pie -o selective_app
# 开始测试
python3 ../scripts/batch_test.py
# 性能测试
python3 ../scripts/perf_test.py
```

## 多线程/递归稳定性测试
``` bash
cd ~/DynamicCanaryProject/test/output
# 生成未受保护的纯净 LLVM IR
clang -O0 -Xclang -disable-O0-optnone -S -emit-llvm ../src/test_boundary.c -o test_boundary.ll
# 生成受 LLVM Pass 插件进行拦截与插桩的 LLVM IR
opt -S -load-pass-plugin=../../build/libDynamicCanary.so -passes="dyn-canary" < test_boundary.ll > test_boundary_prot.ll
# 将插桩后的 IR 代码与运行时库共同链接，生成可执行文件（注意需要链接 pthread 库）
clang -O0 test_boundary_prot.ll ../../runtime/canary_rt.o ../../runtime/canary_log.o -o boundary_app -lpthread
# 运行成果
./boundary_app
```

## 攻击测试
``` bash
cd ~/DynamicCanaryProject/test/output
# 为了让所有攻击手法都能被验证，我们在编译**基线版本（无防护）**时，需要手动关闭现代编译器的默认保护（关闭 PIE，开启栈可执行 -z execstack）。
# 编译无防护的基线版本 (脆弱的靶机)
clang -O0 -fno-stack-protector -z execstack -no-pie ../src/test_targets.c -o targets_baseline
# 生成 IR
clang -O0 -Xclang -disable-O0-optnone -S -emit-llvm ../src/test_targets.c -o test_targets.ll
# 注入动态 Canary (你会看到 3 个函数被成功插桩的绿字提示)
opt -S -load-pass-plugin=../../build/libDynamicCanary.so -passes="dyn-canary" < test_targets.ll > test_targets_prot.ll
# 编译为最终可执行文件 (同样保持栈可执行和无 PIE，以证明防住攻击的纯粹是 Canary 的功劳)
clang -O0 -z execstack -no-pie test_targets_prot.ll ../../runtime/canary_rt.o ../../runtime/canary_log.o -o targets_protected
```
### 测试 1：Ret2Text 对抗
``` bash
cd ~/DynamicCanaryProject/test/scripts
python3 exp_ret2text.py         # 攻击基线，预期输出后门警告
python3 exp_ret2text.py prot    # 攻击保护版本，预期输出 Canary 拦截警告
```
### 测试 2：Ret2Shellcode 对抗
``` bash
cd ~/DynamicCanaryProject/test/scripts
python3 exp_shellcode.py        # 攻击基线，预期输出 shell 成功执行的提示
python3 exp_shellcode.py prot   # 攻击保护版本，预期输出 Canary 拦截警告
```
### 测试 3：Ret2Libc 对抗
``` bash
cd ~/DynamicCanaryProject/test/scripts
python3 exp_ret2libc.py
python3 exp_ret2libc.py prot
```
### 测试 4：ROP 链对抗
``` bash
cd ~/DynamicCanaryProject/test/scripts
python3 exp_rop.py
python3 exp_rop.py prot
```
### 测试 5：Off-By-One 对抗
``` bash
cd ~/DynamicCanaryProject/test/scripts
python3 exp_offbyone.py
python3 exp_offbyone.py prot
```

## 对照组测试
``` bash
cd ~/DynamicCanaryProject/test/output
# 编译基线版
clang -O0 -fstack-protector -no-pie ../src/test_bypass.c -o bypass_baseline   # 主动开启编译器自带的静态 Canary 保护（-fstack-protector）
# 编译保护版，关闭原生保护，注入动态canary
clang -S -emit-llvm -O0 -Xclang -disable-O0-optnone ../src/test_bypass.c -o test_bypass.ll
opt -S -load-pass-plugin=../../build/libDynamicCanary.so -passes="dyn-canary" < test_bypass.ll > test_bypass_prot.ll
# 注意这里用 -fno-stack-protector 确保只有动态 Canary 在工作
clang -O0 -fno-stack-protector -no-pie test_bypass_prot.ll ../../runtime/canary_rt.o ../../runtime/canary_log.o -o bypass_protected
# 运行脚本
cd ~/DynamicCanaryProject/test/scripts
python3 exp_bypass.py
python3 exp_bypass.py prot
```