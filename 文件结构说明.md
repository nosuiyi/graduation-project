# DynamicCanaryProject 文件结构说明

> 本文档对项目所有核心代码文件逐一说明其功能定位、内部结构与设计意图，  
> 适合在阅读源码前作为导览，也适合在代码评审时作为参考索引。

---

## 目录

- [DynamicCanaryProject 文件结构说明](#dynamiccanaryproject-文件结构说明)
  - [目录](#目录)
  - [项目整体架构概览](#项目整体架构概览)
  - [一、构建配置](#一构建配置)
    - [CMakeLists.txt](#cmakeliststxt)
      - [构建目标一：`DynamicCanary`（共享库 `.so`）](#构建目标一dynamiccanary共享库-so)
      - [构建目标二：`canary_rt`（静态库 `.a`）](#构建目标二canary_rt静态库-a)
  - [二、编译期插桩 Pass](#二编译期插桩-pass)
    - [pass/DynamicCanary.cpp](#passdynamiccanarycpp)
      - [核心数据结构](#核心数据结构)
      - [关键方法详解](#关键方法详解)
      - [插件注册机制](#插件注册机制)
  - [三、运行时库](#三运行时库)
    - [runtime/canary\_log.h](#runtimecanary_logh)
      - [关键定义](#关键定义)
    - [runtime/canary\_log.c](#runtimecanary_logc)
      - [模块内部状态](#模块内部状态)
      - [内部辅助函数](#内部辅助函数)
      - [公共接口实现](#公共接口实现)
    - [runtime/canary\_rt.c](#runtimecanary_rtc)
      - [常量](#常量)
      - [线程本地状态（`__thread`）](#线程本地状态__thread)
      - [初始化函数 `__init_dynamic_canary()`（`constructor`，默认优先级）](#初始化函数-__init_dynamic_canaryconstructor默认优先级)
      - [核心运行时函数](#核心运行时函数)
  - [四、测试靶场源文件（test/src/）](#四测试靶场源文件testsrc)
    - [test\_selective.c](#test_selectivec)
    - [test\_vuln.c](#test_vulnc)
    - [test\_bypass.c](#test_bypassc)
    - [test\_targets.c](#test_targetsc)
    - [test\_boundary.c](#test_boundaryc)
    - [benchmark.c](#benchmarkc)
  - [五、测试驱动脚本（test/scripts/）](#五测试驱动脚本testscripts)
    - [batch\_test.py](#batch_testpy)
    - [perf\_test.py](#perf_testpy)
    - [exp.py](#exppy)
    - [exp\_bypass.py](#exp_bypasspy)
    - [exp\_ret2text.py](#exp_ret2textpy)
    - [exp\_ret2libc.py](#exp_ret2libcpy)
    - [exp\_rop.py](#exp_roppy)
    - [exp\_shellcode.py](#exp_shellcodepy)
    - [exp\_offbyone.py](#exp_offbyonepy)
  - [六、文件依赖关系速查](#六文件依赖关系速查)

---

## 项目整体架构概览

本项目实现了一套基于 LLVM Pass 的**动态金丝雀（Dynamic Canary）选择性栈保护方案**，由三层结构组成：

```
┌─────────────────────────────────────────────────────┐
│  编译期（Compile-Time）                              │
│  pass/DynamicCanary.cpp                             │
│  └─ 分析每个函数的 alloca 指令 → 判断是否高风险       │
│  └─ 高风险函数：注入 __push / __check 调用           │
│  └─ 低风险函数：原样放行，零额外开销                  │
└──────────────────────┬──────────────────────────────┘
                       │ 生成调用指令（LLVM IR 级别）
┌──────────────────────▼──────────────────────────────┐
│  运行时（Runtime）                                   │
│  runtime/canary_rt.c   ← 影子栈 + Canary 生成/校验   │
│  runtime/canary_log.c  ← 告警输出 + 日志持久化       │
│  runtime/canary_log.h  ← 公共类型与接口声明          │
└──────────────────────┬──────────────────────────────┘
                       │ 链接到目标程序
┌──────────────────────▼──────────────────────────────┐
│  测试验证（Testing）                                 │
│  test/src/     ← 靶场程序（含真实漏洞，供攻击）       │
│  test/scripts/ ← 自动化验证脚本（pwntools 驱动）     │
└─────────────────────────────────────────────────────┘
```

---

## 一、构建配置

### CMakeLists.txt

**路径：** `CMakeLists.txt`  
**性质：** CMake 构建脚本

管理整个项目的两个构建产物：

#### 构建目标一：`DynamicCanary`（共享库 `.so`）

- 源文件：`pass/DynamicCanary.cpp`
- 产物：`build/libDynamicCanary.so`
- 编译参数：`-fno-rtti`（与 LLVM 的 RTTI 设置保持一致，防止 ABI 不兼容导致链接失败）
- 用途：
  - 作为 `opt` 的插件：`opt --load-pass-plugin=build/libDynamicCanary.so -passes="dyn-canary"`
  - 作为 `clang` 的插件：`clang -fpass-plugin=build/libDynamicCanary.so`（自动注入模式）

#### 构建目标二：`canary_rt`（静态库 `.a`）

- 源文件：`runtime/canary_rt.c`（隐式依赖 `canary_log.c`）
- 产物：`build/libcanary_rt.a`
- 用途：链接到被保护的目标程序，提供 `__push_dynamic_canary` 和 `__check_dynamic_canary` 的具体实现

---

## 二、编译期插桩 Pass

### pass/DynamicCanary.cpp

**路径：** `pass/DynamicCanary.cpp`  
**性质：** LLVM New Pass Manager（NPM）Function Pass，C++ 实现  
**行数：** 175 行

这是整个方案的"大脑"，在编译阶段完成所有插桩决策和代码注入，运行时零开销于此函数。

#### 核心数据结构

- **`DynamicCanaryPass`**：继承自 `PassInfoMixin<DynamicCanaryPass>`，是 NPM 框架下的标准 Function Pass 结构体。

#### 关键方法详解

**`shouldInstrument(Function &F) → bool`**

选择性插桩的过滤器，决定某个函数是否需要保护：

| 检查项 | 判断逻辑 | 决策 |
|---|---|---|
| 函数无任何 alloca | 无局部变量 | 放行（不插桩） |
| alloca 类型为标量或小型数组（< 8 字节） | 溢出风险极低 | 放行 |
| alloca 为数组且总大小 ≥ 8 字节 | 缓冲区溢出高风险 | **触发插桩** |
| alloca 为动态大小（VLA / 非静态） | 运行时大小不可预测 | **触发插桩** |

大小计算通过 `DataLayout::getTypeAllocSize()` 精确获取，避免对齐补全导致的误判。

**`run(Function &F, FunctionAnalysisManager &FAM) → PreservedAnalyses`**

Pass 的主执行函数，仅在 `shouldInstrument` 返回 `true` 时执行：

1. **准备阶段**：获取 `LLVMContext`、构造 `IRBuilder`，声明两个外部运行时函数符号（`__push_dynamic_canary`、`__check_dynamic_canary`）。
2. **FuncID 计算**：使用 `llvm::hash_value(F.getName())` 将函数名映射为 `uint64_t`，作为编译期常量注入 IR，保证不同函数生成不同的 Canary 值。
3. **Prologue 插桩**（函数入口第一条可插入指令处）：
   - `CreateAlloca` → 在栈帧上开辟 `dyn_canary_slot`（8 字节）
   - `CreateCall(__push_dynamic_canary, FuncID)` → 调用运行时生成 Canary
   - `CreateStore` → 将返回的 Canary 明文写入 `dyn_canary_slot`
4. **Epilogue 插桩**（遍历所有 BasicBlock，找到 `ReturnInst` 的前一条插入位置）：
   - `CreateLoad` → 从 `dyn_canary_slot` 读取 Canary（此时若溢出已发生，值可能被篡改）
   - `CreateCall(__check_dynamic_canary, FuncID, StoredCanary)` → 传给运行时校验

#### 插件注册机制

- **命名 Pass 注册**（`registerPipelineParsingCallback`）：将 Pass 注册为名为 `"dyn-canary"` 的 Pass，可通过 `opt -passes="dyn-canary"` 在测试中单独调用。
- **流水线自动注入**（`registerPipelineStartEPCallback`）：挂载到 `PipelineStartEP`，当使用 `clang -O0/-O2` 等任意优化级别编译时自动触发，无需用户显式指定 Pass 名称，实现对真实项目的透明保护。

---

## 三、运行时库

### runtime/canary_log.h

**路径：** `runtime/canary_log.h`  
**性质：** C 头文件，日志模块的公共接口声明  
**行数：** 58 行

定义了日志与告警模块所有对外暴露的类型和函数签名，被 `canary_rt.c` 和 `canary_log.c` 共同包含。

#### 关键定义

**宏**
- `CANARY_DEFAULT_LOG_PATH`：日志文件的默认写入路径（`/home/user/DynamicCanaryProject/canary_alert.log`），可通过环境变量 `CANARY_LOG_PATH` 覆盖。

**枚举 `CanaryEventType`**

| 枚举值 | 含义 |
|---|---|
| `CANARY_EVENT_STACK_OVERFLOW` (1) | 标准栈溢出：函数出口处 Canary 校验失败，栈帧被篡改 |
| `CANARY_EVENT_SHADOW_OVERFLOW` (2) | 影子栈上溢：调用深度超过 `MAX_DEPTH`（1024 层） |
| `CANARY_EVENT_SHADOW_UNDERFLOW` (3) | 影子栈下溢：`push`/`pop` 次数不匹配（运行时环境异常） |

**结构体 `CanaryAlertInfo`**

一条告警事件的完整信息载体，由运行时在触发告警时填充：

| 字段 | 类型 | 含义 |
|---|---|---|
| `event_type` | `CanaryEventType` | 事件分类 |
| `func_id` | `uint64_t` | 出问题的函数哈希 ID |
| `stored_canary` | `uint64_t` | 从栈帧读出的 Canary（可能已被攻击者覆盖） |
| `expected_canary` | `uint64_t` | 影子栈中存储的预期值 |
| `shadow_depth` | `int` | 异常发生时影子栈的深度 |
| `tid` | `pid_t` | 触发异常的线程 ID |

**函数声明**
- `__canary_alert(const CanaryAlertInfo *)` → `noreturn`：唯一的告警入口，负责输出 stderr 告警框、写日志文件、调用 `abort()`。
- `__init_canary_log(void)`：日志模块初始化，由 `constructor(101)` 在 `main()` 前自动调用。

---

### runtime/canary_log.c

**路径：** `runtime/canary_log.c`  
**性质：** C 源文件，日志模块实现  
**行数：** 189 行

实现了检测到攻击后的完整响应流程：即时告警 → 持久化日志 → 进程终止。

#### 模块内部状态

- `g_log_path[256]`（`static`）：日志文件的实际写入路径，在初始化阶段确定后只读，线程安全。

#### 内部辅助函数

**`EventTypeToString(CanaryEventType) → const char *`**

将事件类型枚举转为可读字符串（`"STACK_OVERFLOW"` 等），返回静态字符串，无需 `free()`，用于日志格式化。

**`GetCurrentTimeString(char *buf, size_t buf_len)`**

封装 `localtime` + `strftime`，生成 `"YYYY-MM-DD HH:MM:SS"` 格式的时间字符串，带兜底处理（`localtime` 失败时写入 `"unknown-time"`）。

**`WriteAlertToFile(const CanaryAlertInfo *info)`**

以**追加模式**（`"a"`）打开日志文件，写入一条结构化日志条目：

```
[2026-04-15 10:23:47] EVENT=STACK_OVERFLOW        TID=12345
  FUNC_ID         = 0xabcdef1234567890
  STORED_CANARY   = 0xdeadbeefcafebabe
  EXPECTED_CANARY = 0xffc9493eade7e0e3
  SHADOW_DEPTH    = 3
```

追加模式在 Linux 下对单次 `write()` 系统调用是原子的，多进程同时写入不会产生数据交错。

#### 公共接口实现

**`__init_canary_log()`**（`constructor(101)`）

优先级 `101` 高于 `canary_rt.c` 中默认优先级的 `constructor`，确保日志模块先于运行时核心就绪。执行：
1. 读取环境变量 `CANARY_LOG_PATH`，若有效则覆盖 `g_log_path`。
2. 向日志文件追加写入会话头标记（含 PID 与启动时间），便于区分不同运行实例。

**`__canary_alert(const CanaryAlertInfo *)`**（`noreturn`）

三步响应：
1. 向 `stderr` 输出带 Unicode 边框的人类友好告警框（即时可见，不受 stdout 缓冲影响）。
2. 调用 `WriteAlertToFile()` 将结构化事件写入日志文件（持久化，便于事后审计）。
3. 调用 `abort()`（而非 `exit()`）：触发 `SIGABRT`，操作系统生成 core dump，保留完整现场供取证分析。

---

### runtime/canary_rt.c

**路径：** `runtime/canary_rt.c`  
**性质：** C 源文件，运行时核心模块  
**行数：** 195 行

实现了动态 Canary 机制的两个关键运行时函数，是整个防御方案的执行核心。

#### 常量

- `MAX_DEPTH 1024`：影子栈最大深度，即支持最深 1024 层的函数嵌套调用。超出后触发 `CANARY_EVENT_SHADOW_OVERFLOW` 告警。

#### 线程本地状态（`__thread`）

所有状态变量均使用 `__thread` 修饰，每个线程拥有完全独立的副本，从根本上消除多线程竞态条件：

| 变量 | 类型 | 作用 |
|---|---|---|
| `thread_local_seed` | `uint64_t` | Canary 生成种子，进程启动时从 `/dev/urandom` 读取真随机数初始化 |
| `call_counter` | `uint64_t` | 单调递增调用计数器，保证同一函数每次调用生成不同 Canary，抵抗"泄露后重用"攻击 |
| `shadow_canary_stack[MAX_DEPTH]` | `uint64_t[]` | 影子栈本体，以加密形式存储每次调用的期望 Canary 值 |
| `shadow_top` | `int` | 影子栈栈顶指针（下一个可写槽位的索引） |
| `shadow_cookie` | `uint64_t` | 影子栈 XOR 加密密钥，独立于 `seed` 单独随机，防止攻击者推算密文 |

#### 初始化函数 `__init_dynamic_canary()`（`constructor`，默认优先级）

在 `main()` 执行前自动调用，完成两项工作：
- 从 `/dev/urandom` 读取 8 字节初始化 `thread_local_seed`
- 从 `/dev/urandom` 再次独立读取 8 字节初始化 `shadow_cookie`（与 seed 统计独立）
- 降级兜底：`/dev/urandom` 不可用时使用 `time(NULL) ^ &thread_local_seed` 作为弱随机种子

#### 核心运行时函数

**`__push_dynamic_canary(uint64_t func_id) → uint64_t`**

在被保护函数**入口**处由插桩代码调用：

```
call_counter++
canary = thread_local_seed XOR func_id XOR call_counter   ← 三元 XOR，每次唯一
shadow_canary_stack[shadow_top++] = canary XOR shadow_cookie  ← 加密后压栈
return canary   ← 明文返回，LLVM Pass 将其存入函数栈帧局部变量
```

三元 XOR 的安全含义：
- `seed` 不可预知（真随机）
- `func_id` 与函数绑定（泄露 A 函数的 Canary 无法用于 B 函数）
- `call_counter` 单调递增（先泄露再攻击时值已变化）

**`__check_dynamic_canary(uint64_t func_id, uint64_t stored_canary)`**

在被保护函数**出口**处由插桩代码调用：

```
expected = shadow_canary_stack[--shadow_top] XOR shadow_cookie   ← 解密弹栈
if expected != stored_canary → __canary_alert(STACK_OVERFLOW)    ← 不匹配则告警
// 匹配则正常返回
```

`stored_canary` 是从函数栈帧局部变量读取的值，若发生溢出攻击覆盖了栈帧，该值会与 `expected` 不符，从而被精确检测。

---

## 四、测试靶场源文件（test/src/）

### test_selective.c

**路径：** `test/src/test_selective.c`  
**行数：** 52 行  
**配套脚本：** `batch_test.py`  
**用途：** 验证选择性插桩策略的正确性

设计了四个函数，精确覆盖 Pass 的全部判断分支，确保"应插桩的插了，不应插桩的没有插"：

| 函数 | 局部变量情况 | 预期插桩结果 | 验证意图 |
|---|---|---|---|
| `safe_math()` | 仅有标量 `int`，无数组 | **不插桩** | 纯计算函数零开销 |
| `small_buffer_func()` | `char flag[4]`（4 字节，< 8 字节阈值） | **不插桩** | 小缓冲区低风险放行 |
| `risky_buffer_func()` | `char buffer[64]`（64 字节，≥ 8 字节） | **插桩** | 大缓冲区高风险保护 |
| `main()` | 无数组 | **不插桩** | 主函数安全放行 |

`risky_buffer_func()` 故意允许 `read(0, buffer, 128)` 读入 128 字节到 64 字节缓冲区，制造真实溢出漏洞，供 `batch_test.py` 第二阶段动态触发。

---

### test_vuln.c

**路径：** `test/src/test_vuln.c`  
**行数：** 50 行  
**配套脚本：** `exp.py`  
**用途：** 基础 Ret2Text 攻击对比验证

最简化的漏洞靶场，剥离所有干扰，专注于验证"有无 Canary 保护时 Ret2Text 攻击的结果差异"：

- **`hacked()`**：后门函数，控制流跳入此处即宣告攻击成功，使用 `puts()` 避免 64 位下 `printf` 栈对齐崩溃。
- **`vulnerable_func()`**：`buffer[8]` 但允许 `read(0, buffer, 64)`，经典的 8 倍溢出；含数组故会被 Pass 插桩保护。

编译为两个版本：
- `vuln_app`：无保护，攻击预期成功
- `protected_app`：动态 Canary 保护，攻击预期被拦截

---

### test_bypass.c

**路径：** `test/src/test_bypass.c`  
**行数：** 82 行  
**配套脚本：** `exp_bypass.py`  
**用途：** 验证动态 Canary 对"泄露后重用"绕过攻击的抵御能力

设计了两个协作漏洞，模拟真实场景中攻击者将多个漏洞串联使用的两步攻击链：

- **`echo_service()`**（漏洞一）：`char buf[64]`，但使用 `printf(buf)` 直接打印用户输入，导致格式化字符串漏洞。攻击者通过 `%15$p`（动态 Canary）或 `%19$p`（静态 Canary）读取栈上的 Canary 值。
- **`vuln_service()`**（漏洞二）：`char buf[32]` 但允许读入 256 字节，标准栈溢出。攻击者将泄露的 Canary 填入 Payload 的对应位置，企图伪造合法帧。

防御对比：
- **静态 Canary 基线版**：Canary 固定不变，泄露后原样填回即可绕过，攻击成功。
- **动态 Canary 保护版**：下一次调用时 Canary 已因 `call_counter` 自增而改变，泄露的旧值失效，校验失败，攻击被拦截。

---

### test_targets.c

**路径：** `test/src/test_targets.c`  
**行数：** 113 行  
**配套脚本：** `exp_ret2text.py`、`exp_ret2libc.py`、`exp_rop.py`、`exp_shellcode.py`、`exp_offbyone.py`  
**用途：** 多攻击手法综合靶场，五个脚本共用同一份靶场源码

通过命令行参数 `argv[1]` 选择靶场编号，集成三种经典栈漏洞：

**辅助符号（所有靶场共用）：**
- `backdoor()`：Ret2Text 的目标后门函数。
- `sh_str = "/bin/sh"`：全局字符串，供 Ret2Libc 和 ROP 攻击使用。
- `rop_gadget()`：内嵌 `__asm__("pop %rdi; ret")`，向二进制中注入一个 `pop rdi; ret` Gadget。

**靶场 1 —— `vuln_standard()`（`argv[1] = "1"`）：**
- `buf[32]` 允许读取 256 字节，经典栈溢出
- 主动打印 `system()` 和 `sh_str` 的运行时地址（规避 ASLR，让测试专注于 Canary 攻防）
- 被 `exp_ret2text.py`、`exp_ret2libc.py`、`exp_rop.py` 共用

**靶场 2 —— `vuln_shellcode()`（`argv[1] = "2"`）：**
- `buf[64]` 允许读取 256 字节
- 主动打印 `buf` 的栈地址（绕过 ASLR，供 shellcode 精准跳转）
- 被 `exp_shellcode.py` 使用；编译时需关闭 NX（`-z execstack`）

**靶场 3 —— `vuln_off_by_one()`（`argv[1] = "3"`）：**
- `buf[32]` + `buf[n] = '\0'`：仅在末尾补写一个空字节，产生单字节越界
- 精确覆盖紧挨 `buf` 上方的 `saved RBP` 最低位字节，导致函数返回时栈指针偏移（栈帧迁移）
- 被 `exp_offbyone.py` 使用

---

### test_boundary.c

**路径：** `test/src/test_boundary.c`  
**行数：** 77 行  
**用途：** 对动态 Canary 运行时进行极端边界场景的压力测试

**场景 1 —— 深度递归（`deep_recursion(1000)`）：**
- 函数含 `char buffer[16]`（触发插桩），每层调用都会执行一次 `__push_dynamic_canary` + `__check_dynamic_canary`
- 1000 层嵌套会将影子栈压入/弹出 1000 次，验证 `shadow_top` 的边界管理（`MAX_DEPTH = 1024`，1000 < 1024，预期正常通过）
- 验证深层嵌套时 Canary 的入栈/出栈顺序与函数调用顺序严格对应，不发生错配

**场景 2 —— 多线程并发（5 个线程）：**
- `thread_worker()` 含 `char local_buf[32]`（触发插桩）
- 5 个线程同时运行，每线程有独立的 `__thread` 状态（`seed`、`shadow_stack`、`counter`、`cookie`）
- 验证 TLS 隔离的有效性：线程间的 Canary 操作互不干扰，无竞态条件

---

### benchmark.c

**路径：** `test/src/benchmark.c`  
**行数：** 61 行  
**配套脚本：** `perf_test.py`  
**用途：** 性能基准测试的被测目标程序

设计了两个函数，模拟"10:1 安全/危险调用比"的真实负载：

- **`safe_math_compute(int n)`**（`noinline`）：纯算术运算（求和 + XOR），无数组，**不会被 Pass 插桩**，代表零开销路径。
- **`risky_buffer_compute(int seed)`**（`noinline`）：`char buffer[64]`，填充并求校验和，**会被 Pass 插桩**，代表有保护开销的路径。

编译控制要点：
- `__attribute__((noinline))`：防止内联，确保 Pass 能独立分析每个函数。
- 函数有返回值（校验和）：对抗编译器的死代码消除（DCE），保证高优化级别下数组操作不被消除，使测量结果有效。
- 循环嵌套（`OUTER_LOOPS=100000 × INNER_LOOPS=1000`）：产生足够的运行时长，降低测量误差。

---

## 五、测试驱动脚本（test/scripts/）

### batch_test.py

**路径：** `test/scripts/batch_test.py`  
**行数：** 86 行  
**依赖：** pwntools、objdump  
**用途：** 选择性插桩方案的端到端自动化验证，是最主要的功能验证入口

分两个阶段完整验证方案：

**阶段一：静态插桩验证**
- 使用 `os.popen("objdump -d ... | awk ...")`  对 `selective_app` 进行反汇编
- 对 `['safe_math', 'small_buffer_func', 'risky_buffer_func', 'main']` 逐一检查汇编代码中是否存在 `<__push_dynamic_canary>` 的调用
- 输出带符号的验证结果（`🛡️` / `⚪`），直观反映插桩策略是否符合预期

**阶段二：动态溢出测试**
- 使用 `process(binary_path, stderr=STDOUT)` 启动程序，**合并 stderr 到 stdout**（关键：告警输出到 stderr，必须合并才能捕获）
- 发送 100 字节 Payload（超过 64 字节缓冲区），触发 `risky_buffer_func` 的栈溢出
- `recvall(timeout=3)` 接收全部输出，检测是否包含 `"检测到攻击"` 或 `"[!!!]"`
- 检测到告警后，提取并打印完整的 Unicode 告警框（从 `╔` 到 `╚`）

---

### perf_test.py

**路径：** `test/scripts/perf_test.py`  
**行数：** 289 行  
**依赖：** clang、opt、`/usr/bin/time`（Linux）  
**用途：** 量化动态 Canary 防御方案的性能开销，支撑方案可行性评估

分四个阶段对**基线版本**（无保护）与**保护版本**（动态 Canary 插桩）进行系统性对比：

| 阶段 | 度量指标 | 测量方法 | 重复次数 |
|---|---|---|---|
| 零（准备） | 生成统一 LLVM IR | `clang -S -emit-llvm -Xclang -disable-O0-optnone` | — |
| 一 | 编译时间 | `time(os.system(cmd))` | 3 次取均值 |
| 二 | 二进制体积 | `os.path.getsize()` | — |
| 三 | 运行时执行时间 | `time.time()` 包裹 `subprocess.run()` | 10 次，去极值取均值 |
| 四 | 峰值内存（RSS） | `/usr/bin/time -v` 解析 `Maximum resident set size` | 5 次取均值 |

核心设计原则：两个版本**从同一份 LLVM IR 编译**，消除编译差异干扰，性能差异仅来源于插桩本身。

工具函数：
- `measure_execution_time()`：去极值均值，抗系统调度抖动
- `measure_peak_memory_kb()`：RSS 多次均值，带 `/usr/bin/time` 不可用的降级处理
- `measure_compile_time_seconds()`：编译耗时均值
- `format_overhead(base, prot)`：计算并格式化开销百分比（`+12.34%` 格式）

---

### exp.py

**路径：** `test/scripts/exp.py`  
**行数：** 58 行  
**靶场：** `test_vuln.c`  
**用途：** 验证最基础的 Ret2Text 攻击在有无保护时的对比结果

封装了 `exploit(binary_name)` 函数，对 `vuln_app` 和 `protected_app` 各发起一次攻击：

攻击流程：
1. 用 `ELF()` 解析二进制，读取 `hacked` 符号的固定地址
2. 构造 Payload：`b"A" * 16 + p64(hacked_addr)`（16 字节偏移 = `buffer[8]` + 对齐填充 + saved RBP 的一部分）
3. `recvuntil(b": ")` 同步后 `send(payload)`
4. 打印全部输出，有保护版本预期看到 Canary 告警

---

### exp_bypass.py

**路径：** `test/scripts/exp_bypass.py`  
**行数：** 97 行  
**靶场：** `test_bypass.c`（编译为 `bypass_baseline` / `bypass_protected`）  
**用法：** `python3 exp_bypass.py [prot]`  
**用途：** 验证动态 Canary 对"泄露后重用"绕过攻击的抵御能力

两阶段攻击的精确实现：

**战术一（信息泄露）：**
- 选择菜单 `1`（Echo 服务）
- 发送格式化字符串：`%15$p`（动态版）或 `%19$p`（静态版），通过 `printf` 漏洞打印栈上指定偏移处的值
- 解析十六进制输出，得到 Canary 值

**战术二（伪造现场）：**
- 选择菜单 `2`（Vuln 服务）
- 构造 Payload：`b'A' * 40 + p64(leaked_canary) + b'B' * 8 + p64(backdoor_addr)`
- 将泄露的 Canary 原样填入栈帧的 Canary 位置，企图骗过校验
- 静态版因 Canary 不变而成功；动态版因 `call_counter` 已变化而失败

---

### exp_ret2text.py

**路径：** `test/scripts/exp_ret2text.py`  
**行数：** 42 行  
**靶场：** `test_targets.c`（靶场 1）  
**用法：** `python3 exp_ret2text.py [prot]`  
**用途：** 最简 Ret2Text 攻击对比验证（targets 版）

- 用 `elf.symbols['backdoor']` 解析后门地址
- 基线版 Payload：`b'A' * 40 + p64(backdoor_addr)`（`buf[32]` + 8 字节 RBP = 40 字节偏移）
- 保护版 Payload：`b'A' * 64 + p64(backdoor_addr)`（插桩后栈帧被扩展，需要更长的填充才能覆盖 Canary）

---

### exp_ret2libc.py

**路径：** `test/scripts/exp_ret2libc.py`  
**行数：** 59 行  
**靶场：** `test_targets.c`（靶场 1）  
**用法：** `python3 exp_ret2libc.py [prot]`  
**用途：** Ret2Libc 攻击验证，利用程序泄露的运行时地址规避 ASLR

攻击流程：
1. 接收程序打印的 `system()` 和 `sh_str` 运行时地址
2. 用 `elf.search(asm('pop rdi; ret'))` 定位 `pop rdi; ret` Gadget
3. 用 `elf.search(asm('ret'))` 定位纯 `ret` Gadget（用于 16 字节栈对齐）
4. 构造 ROP 链：`padding(40) + ret(对齐) + pop_rdi + sh_str + system`
5. 保护版直接发送 100 字节暴力填充，触发 Canary 拦截

---

### exp_rop.py

**路径：** `test/scripts/exp_rop.py`  
**行数：** 58 行  
**靶场：** `test_targets.c`（靶场 1）  
**用法：** `python3 exp_rop.py [prot]`  
**用途：** 高级两段式 ROP 链攻击验证

在 `exp_ret2libc.py` 基础上构造更复杂的两段连续 ROP：
1. 第一段：`ret + pop_rdi + sh_str + printf`（验证 ROP 链控制流可达，打印 `/bin/sh`）
2. 第二段：`ret + pop_rdi + sh_str + system`（实际弹出 Shell）

用 `elf.plt['printf']` 获取 `printf` 的 PLT 地址。测试 Canary 在面对复杂多跳 ROP 时的拦截能力。

---

### exp_shellcode.py

**路径：** `test/scripts/exp_shellcode.py`  
**行数：** 61 行  
**靶场：** `test_targets.c`（靶场 2，需 `-z execstack`）  
**用法：** `python3 exp_shellcode.py [prot]`  
**用途：** Ret2Shellcode 攻击验证，测试注入执行任意机器码的防御能力

攻击流程：
1. 接收程序打印的 `buf` 栈地址（程序主动泄露，绕过 ASLR）
2. 生成 shellcode：`asm("sub rsp, 0x100") + asm(shellcraft.sh())`（开头的 `sub rsp` 将栈指针下移 0x100 字节，防止后续 `execve` 系统调用覆盖自身）
3. 构造 Payload：`shellcode.ljust(72, b'A') + p64(buf_addr)`（将 shellcode 注入缓冲区，返回地址指向缓冲区起始）
4. 注入后发送 `echo` 命令验证 Shell 是否取得

---

### exp_offbyone.py

**路径：** `test/scripts/exp_offbyone.py`  
**行数：** 39 行  
**靶场：** `test_targets.c`（靶场 3）  
**用法：** `python3 exp_offbyone.py [prot]`  
**用途：** Off-By-One 单字节溢出攻击验证

攻击手法极为精巧，仅需一个字节的越界写：
- 发送恰好 32 字节的 Payload
- 程序在 `buf[32]` 写入 `'\0'`，精确将紧邻的 `saved RBP` 最低位字节清零
- 函数 `leave` 指令执行时，`RSP` 被设置为被篡改的 `RBP` 值，导致栈帧迁移，引发崩溃或可控跳转
- 有 Canary 保护时，单字节写入也会影响 Canary 槽的值（内存相邻），校验失败触发拦截

---

## 六、文件依赖关系速查

```
CMakeLists.txt
├── pass/DynamicCanary.cpp          → 编译为 libDynamicCanary.so
│     └── 运行时调用（LLVM IR 级别插入）
│           ├── __push_dynamic_canary()   ← runtime/canary_rt.c
│           └── __check_dynamic_canary()  ← runtime/canary_rt.c
│
└── runtime/canary_rt.c             → 编译为 libcanary_rt.a
      ├── #include "canary_log.h"   ← runtime/canary_log.h（类型声明）
      └── __canary_alert()          ← runtime/canary_log.c（实现）

test/src/
├── test_selective.c  ←→  test/scripts/batch_test.py
├── test_vuln.c       ←→  test/scripts/exp.py
├── test_bypass.c     ←→  test/scripts/exp_bypass.py
├── test_targets.c    ←→  test/scripts/exp_ret2text.py
│                         test/scripts/exp_ret2libc.py
│                         test/scripts/exp_rop.py
│                         test/scripts/exp_shellcode.py
│                         test/scripts/exp_offbyone.py
├── test_boundary.c   （手动运行，无配套脚本）
└── benchmark.c       ←→  test/scripts/perf_test.py
```

---

*文档生成日期：2026-04-22*  
*作者：陈文嘉*
