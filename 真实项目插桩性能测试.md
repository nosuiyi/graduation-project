# 真实项目插桩性能测试

本文档记录动态 Canary 插桩方案在真实开源项目上的完整测试流程与实测结果。

---

## 测试方案概述

测试分为两个独立脚本，位于 `test/scripts/` 目录：

| 脚本 | 作用 |
|---|---|
| `build_real_projects.sh` | 编译基线版（无插桩）和保护版（动态 Canary），保存二进制 |
| `run_real_perf_test.sh`  | 对已编译好的二进制执行时间 + 内存性能对比测试 |

**分离的原因**：编译一次耗时约 1~2 分钟，而性能测试需要反复运行。分开后只需编译一次，后续测试直接复用二进制。

---

## 前置准备

### 1. 确认 Pass 插件存在
```bash
ls ~/DynamicCanaryProject/build/libDynamicCanary.so
```

### 2. 确认或编译运行时库
```bash
cd ~/DynamicCanaryProject/runtime
clang -O0 -c canary_rt.c  -o canary_rt.o
clang -O0 -c canary_log.c -o canary_log.o
ls *.o   # 确认 canary_rt.o 和 canary_log.o 都存在
```

---

## 第一步：编译两个版本

```bash
cd ~/DynamicCanaryProject/test/scripts

# 仅编译 gzip
bash build_real_projects.sh gzip

# 仅编译 bzip2（需先下载源码，见下方说明）
bash build_real_projects.sh bzip2

# 编译所有项目
bash build_real_projects.sh
```

**脚本会自动完成**：
1. 检查 Pass 插件和运行时库
2. 编译无插桩的 `gzip_baseline`（`-O2`）
3. 编译有插桩的 `gzip_protected`（`-O2 -fpass-plugin=libDynamicCanary.so`）
4. 打印被插桩的函数列表并验证 canary 符号

**跳过逻辑**：若 `gzip_baseline` 或 `gzip_protected` 已存在，则跳过对应编译，避免重复等待。需要重新编译时，手动删除对应文件后重新运行脚本。

### 关于 bzip2 源码下载

```bash
cd ~/DynamicCanaryProject
wget https://sourceware.org/pub/bzip2/bzip2-1.0.8.tar.gz
tar -zxvf bzip2-1.0.8.tar.gz
```

---

## 第二步：执行性能测试

```bash
cd ~/DynamicCanaryProject/test/scripts

# 仅测试 gzip
bash run_real_perf_test.sh gzip

# 仅测试 bzip2
bash run_real_perf_test.sh bzip2

# 测试所有项目
bash run_real_perf_test.sh
```

测试配置（可在脚本顶部修改）：
- 执行时间测试：各 **30 次**，去掉前后各 10% 极值后取均值
- 内存峰值测试：各 **5 次**，取均值
- 测试文件：**200 MB 随机数据**（`/tmp/test_gzip_200mb.bin`，首次运行自动生成）

---

## 关于 -O2 与插桩的兼容性

Pass 注册使用 `registerPipelineStartEPCallback`，在 LLVM 优化流水线**最早入口点**执行。执行顺序为：

```
clang -O2 -fpass-plugin=libDynamicCanary.so
      │
      ▼
  PipelineStart  ← 我们的 Pass 在此处运行（sroa/mem2reg 之前）
      │
      ▼
  mem2reg / sroa（消除 alloca）
      │
      ▼
  其他 -O2 优化 Pass...
```

**结论**：Pass 在 `sroa` 之前看到完整的 `alloca` 指令，插桩正常有效。两版本均使用 `-O2`，对比数据可信。

---

## gzip-1.13 实测结果

**测试环境**：200 MB 随机数据文件，各运行 30 次（时间，去掉前后各 10% 极值取均值）/ 5 次（内存）

| 指标 | 基线版本 | 保护版本 | 开销 |
|---|---|---|---|
| 执行时间 | 4.1521 秒 | 4.2846 秒 | **+3.19%** |
| 峰值内存 | 1904 KB | 2296 KB | **+20.59%** |

### 结果分析

**执行时间 +3.19%**：极低开销。gzip 的核心计算密集函数（`longest_match`、`deflate` 等）体量远大于 canary 的 TLS 写入开销，被插桩函数只占全部函数的一小部分。

**峰值内存 +20.59%**：绝对增量约 392 KB，来源明确：
- TLS 影子栈 `shadow_canary_stack[1024]` = 8 KB
- Linux TLS 段对齐分配的填充开销
- gzip 本身内存用量极轻（~2 MB），所以相对百分比较高

内存增量是**固定开销**，与程序本身内存用量无关。对于内存用量更大的程序，相对百分比会更低。

---

## bzip2-1.0.8 实测结果

**测试环境**：200 MB 随机数据文件，各运行 30 次（时间，去掉前后各 10% 极值取均值）/ 5 次（内存）

| 指标 | 基线版本 | 保护版本 | 开销 |
|---|---|---|---|
| 执行时间 | 16.8200 秒 | 16.5463 秒 | **≈0%**（测量误差范围内） |
| 峰值内存 | 7792 KB | 8180 KB | **+4.98%** |

### 结果分析

**执行时间 ≈0%**：原始数据差值为 -1.63%，但 bzip2 基线的单次波动标准差约为 ±0.6 s，远大于两者均值差（-0.27 s），结论为**执行时间开销在统计误差范围内，实际影响可忽略不计**。

**峰值内存 +4.98%**：绝对增量约 388 KB，与 gzip 情形相同，来源为 TLS 影子栈及段对齐填充。bzip2 本身内存用量约 7.6 MB，相对百分比显著低于 gzip，印证了内存开销是固定值的判断。

---

## 测试结论

> 在人工构造的极端测试场景下（函数体极轻、每次调用都触发 canary），执行时间开销为 +42.9%，体现了 canary 操作的理论上界。而对真实开源软件的测试（gzip-1.13 和 bzip2-1.0.8）表明，实际执行时间影响不超过 **+3.2%**，内存绝对增量稳定在约 **390 KB**，完全满足实用要求。

---

## 注意事项

- 正常运行时 `canary_alert.log` 不会新增内容——没有真实溢出就不会触发告警，这是预期行为。
- 编译保护版时，终端会显示插桩日志（`[Dynamic Canary] 成功为危险函数插桩 ...`），属正常现象。
- `/tmp` 目录重启后清空，但 `run_real_perf_test.sh` 会在文件不存在时自动重新生成。
